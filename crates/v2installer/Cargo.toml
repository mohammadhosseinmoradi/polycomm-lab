[package]
name = "v2installer"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12", features = ["json", "blocking"] }
zip = "4.3.0"
indicatif = "0.18"
symlink = "0.1.0"
dirs = "6.0.0"